rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /mevites/{id} {
      allow read: if true;
      allow create: if true;
      allow update: if true;
    }
  }
}
